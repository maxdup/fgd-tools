[tox]
envlist = py34,py35,py36,coverage

[testenv]
deps =
    discover
    coverage: coverage
commands =
    discover tests

[testenv:coverage]
commands =
    coverage erase
    coverage run -m unittest discover tests
    coverage report
    coverage html

